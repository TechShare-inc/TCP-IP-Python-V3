# Contributing

## Setup

```bash
pip install -e .[dev,docs]
```

## Coding style

- Use `snake_case` for methods, parameters, and variables
- Use `PascalCase` for class names
- Keep deprecated PascalCase aliases only for compatibility wrappers
