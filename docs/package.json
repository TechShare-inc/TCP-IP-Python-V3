{
  "name": "dobot-api-v3-docs",
  "private": true,
  "scripts": {
    "docs:sync-api": "python build_docs.py --api-only",
    "docs:dev": "python build_docs.py --api-only && vitepress dev .",
    "docs:build": "python build_docs.py --api-only && vitepress build .",
    "docs:preview": "vitepress preview ."
  },
  "devDependencies": {
    "vitepress": "^1.5.0"
  }
}
